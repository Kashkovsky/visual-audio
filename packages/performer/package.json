{
  "name": "@va/app-performer",
  "version": "1.0.0",
  "author": "Denys Kashkovskyi",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "dev": "parcel index.html",
    "copy:build": "cp -R ./dist ../../dist",
    "build": "rm -rf ./dist && parcel build index.html && npm run copy:build",
    "clean:build": "rm -rf .parcel-cache && rm -rf dist"
  },
  "dependencies": {
    "fp-ts": "^2.11.1",
    "@va/engine": "1.0.0",
    "@va/visuals": "1.0.0",
    "@va/components": "1.0.0",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "@grammarly/focal": "^0.8.5"
  },
  "devDependencies": {
    "@types/react": "^17.0.35",
    "@types/react-dom": "^17.0.11",
    "parcel": "^2.2.1",
    "typescript": "^4.4.3"
  },
  "resolutions": {
    "set-value": "4.0.1"
  }
}
